{{extend 'layout.html'}}
<h1>ACESSO AS VENDAS</h1>
<a class="btn btn-secondary btn-success" href="{{=URL('acesso_particao', args=vendedor.projeto)}}">VOLTAR</a>
<a class="btn btn-secondary btn-success" href="{{=URL('inserir_venda', args=vendedor.id)}}">INSERIR VENDA</a>
            <br>{{for row in rows:}} {{pass}}
<div class="table-responsive">
           <table class="table table-striped table-borderless ">
					<thead>
					  <tr>
						<th>NOME</th>
					  </tr>
					</thead>
					<tbody>
						{{for row_sub in rows:}}
					  <tr>
                        <td><a href="{{=URL('acesso_vendas', args=row.id)}}">{{=row.nome_vendedor}}</a></td>
					  </tr>
					  {{pass}}
					</tbody>
				  </table>
</div>
